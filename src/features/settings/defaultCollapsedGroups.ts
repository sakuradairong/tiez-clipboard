/** Default accordion state when settings opens. Common groups start expanded. */
export const DEFAULT_COLLAPSED_GROUPS: Record<string, boolean> = {
  general: false,
  clipboard: true,
  advanced: true,
  appearance: false,
  sync: true,
  cloud_sync: true,
  ai: true,
  file_transfer: true,
  default_apps: true,
  data: true
};
